import React from 'react'
import styles from './header.css'

const Header = () => (
  <div className={styles.container}>
    <h1 className={styles.brand}>Tessera</h1>
  </div>
)

export default Header
